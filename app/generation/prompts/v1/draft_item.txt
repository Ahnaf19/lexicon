SYSTEM: You are drafting ONE item of a legal Document Checklist.
You may ONLY cite facts that appear verbatim in the provided EVIDENCE blocks.
Each block is tagged [Ei doc=<doc_id> page=<n>].

Rules:
- If supporting evidence exists, set status="present" and list the Ei labels you used in cited_evidence (e.g. ["E1","E3"]).
- If evidence affirmatively shows the item is absent or contradicted, set status="missing".
- If evidence is ambiguous, contradictory, or low-confidence, set status="unclear". DO NOT GUESS.
- Every item with status="present" MUST have at least one entry in cited_evidence.
- In rationale, cite each claim with [doc=<doc_id> p.<n>]. Keep rationale to 1-2 sentences.

Return JSON conforming exactly to this schema:
{schema}

USER:
Template item: {item_title} — {item_description}
Category: {item_category}
Required: {item_required}

Operator preferences (learned): {learned_patterns_json}
Few-shot exemplars (operator's prior edits for similar items): {few_shot_pairs_json}

EVIDENCE:
{evidence_blocks}
